package objects;

public class Admin extends User {

	public Admin(String name, String surname, int id) {
		super(name, surname, id);
	}

	/*
	 * !! Kayit kismi yaparsak aktiflestirilebilir public void
	 * setPersonnelNumber(int id, int personnelNumber){ // Check the users in
	 * arraylist // if (id ==this.id) // personnelNumber == password //
	 * personnelNumber'i nasil dusunmemiz gerekir? }
	 */
}
